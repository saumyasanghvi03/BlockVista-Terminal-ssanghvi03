# requirements.txt
# Compatible with Python 3.13.7 and standard pip installation
# Uses kiteconnect==5.0.1 (latest stable version)
# pandas==2.2.3 for Python 3.13.7 compatibility
# No pandas_ta to avoid import errors and conflicts

streamlit==1.28.1
kiteconnect==5.0.1
pandas==2.2.3
plotly==5.15.0
numpy==1.26.4
